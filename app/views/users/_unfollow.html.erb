  <%= form_for(current_user.relationships.find_by_followed_id(@promoter),
             html: { method: :delete }) do |f| %>
  <%= f.submit "УДАЛИТЬ ИЗ ИЗБРАННОГО", class: "btn btn-lg btn-danger" %>
<% end %>