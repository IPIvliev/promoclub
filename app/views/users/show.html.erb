<!-- WRAPPER -->
    <div id="wrapper">

      <!-- PAGE TITLE -->
      <%= render "/shared/bread" %>

      <%= render '/shared/moremenu' %>

      <section id="portfolio" class="container">

        <h2><strong><%= "#{@promoter.name} #{@promoter.surname}" %></strong> <%= "(id: #{@promoter.id})" if @promoter.status == "promo" %></h2>


        <div class="row">
          <p id="notice"><%= notice %></p>
          <div class="col-sm-5 col-md-7">
            <div class="controlls-over product-image">
              <div>
                <%= image_tag @promoter.avatar.url, :class => "img-responsive" %>
              </div>
            </div>

          </div>

          <div class="col-sm-7 col-md-5">
            <% if @promoter == current_user %>
              <%= link_to "Удалить анкету", admin_destroy_user_path(current_user), method: :delete, data: { confirm: "You sure?" }, :class => "btn btn-danger" %><br><br>
            <% end %>

          <% if @promoter.status == "promo" %>
            <% if @promoter == current_user %>
              <h3>Так Ваша анкета выглядит у работодателей</h3>
            <% else %>
              <h3>Вы можете нанять этого промоутера на работу</h3>
            <% end %>
          <% else %>
            <h3>Данные о работодателе</h3>
          <% end %>

            <ul class="list-icon">
              <% if @promoter.status == "promo" %>
                <li><i class='fa fa-certificate'></i> Рейтинг: <%= @promoter.followers.count %> </li>
                <br>
                <li><i class='fa fa-flag'></i> Страна: <%= @promoter.country.name %></li>
                <li><i class='fa fa-bookmark-o'></i> Город: <a href="/users?utf8=%E2%9C%93&q%5Bgender_eq%5D=&q%5Bage_lteq%5D=&q%5Bage_gteq%5D=&q%5Bcountry_id_eq%5D=<%= @promoter.country.id %>&q%5Bstate_id_eq%5D=<%= @promoter.state.id %>&q%5Bcity_id_eq%5D=<%= @promoter.city.id %>&commit=%D0%9D%D0%B0%D0%B9%D1%82%D0%B8"><%= @promoter.city.name %> </a>
                </li>
                
                <br>
                <li><i class='fa fa-male'></i> Пол: <%= @promoter.gender %></li> 
                <li><i class='fa fa-clock-o'></i> Возраст: <%= calculate_age(@promoter.birth) if @promoter.birth? %> лет </li>
                <br>
                <li><i class='fa fa-medkit'></i> Наличие медицинской книжки: <%= medkniga(@promoter.med) %></li>
              <% else %>
                <li><i class='fa fa-flag'></i> Страна: <%= @promoter.country.name %></li>
                <li><i class='fa fa-bookmark-o'></i> Город: <%= @promoter.city.name %> </a></li>
              <% end %>
            </ul>

            <i class='fa fa-phone'></i> Номер телефона: 

            <% if current_user and current_user.status == "agent" %>
              <%= @promoter.phone %>
            <% elsif current_user and current_user.status == "promo"  %>
              <p>Только пользователи в статусе "Работодатель" имеют доступ к базе телефонов.</p>
            <% elsif @promoter.status == "agent" %>
              <%= @promoter.phone %>
            <% else  %>
              <p>Только зарегистрированные пользователи в статусе "Работодатель" имеют доступ к базе телефонов. <a href="/users/sign_up?status=agent">Зарегистрироваться</a></p>
            <% end %>

            
<br><br>
<h3><strong>Дополнительная информация</strong></h3>
<% if @promoter.description.blank? && @promoter.status == "promo" %>
  <p>Промоутер не указал дополнительной информации о себе.</p>
<% elsif @promoter.description.blank? && @promoter.status == "agent" %>
  <p>Работодатель не указал дополнительной информации о себе.</p>
<% else %>
  <p><%= @promoter.description %></p>
<% end %>


<% if current_user && @promoter.status == "promo" && current_user.status == "agent" %>
  <div id="follow_form">
  <% if current_user.following?(@promoter) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
  
<br><br>

          </div>
            <div class="divider"><!-- divider -->
              <i class="fa fa-star"></i>
            </div>
            
            <%= render "/shared/important" %>

      </section>

    </div>
    <!-- /WRAPPER -->