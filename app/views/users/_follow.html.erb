<%= form_for(current_user.relationships.build(followed_id: @promoter.id)) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "ДОБАВИТЬ В ИЗБРАННОЕ", class: "btn btn-lg btn-success" %>
<% end %>