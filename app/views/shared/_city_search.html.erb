            <div class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
              <div class="modal-dialog modal-lg">
                <div class="modal-content">

                  <div class="modal-header"><!-- modal header -->
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h4 class="modal-title" id="myModalLabel">Выбор города</h4>
                  </div><!-- /modal header -->

                  <!-- modal body -->
                  <div class="modal-body">
                    <strong>Выберите город из списка</strong>
                    <p>В данном списке представлены только те города, в которых имеются зарегистрированные промоутеры. В скобках указано количество зарегистрированных промоутеров.</p>
                <ul>

              <% cache 'promo_cities_list' do %>
                <% City.all.each do |city| %>
                  <% if User.where(:city_id => city.id, :status => "promo").count > 0 %>
                    <li><%= link_to "#{city.name} (#{city.users.where(:status => 'promo').count})", promo_city_path(city) %></li>
                  <% end %>
                <% end %>
              <% end %>

                </ul>

                  </div>
                  <!-- /modal body -->

                  <div class="modal-footer"><!-- modal footer -->
                    <p><a href="/baza-promouterov.html">Общий список промоутеров без указания города</a></p>
                  </div><!-- /modal footer -->

                </div>
              </div>
            </div>
